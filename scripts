# Add a DHCP role

Add-WindowsFeature DHCP -IncludeManagementTools

#Pour créer les groupes de gestion DHCP à l'aide de Windows PowerShell, exécutez la commande suivante :

Add-DhcpServerSecurityGroup -Computer DhcpServerName

Add-DHCPServerinDC <hostname or IP address of DHCP server>



Invoke-IpamGpoProvisioning -Domain pharma.cm -GpoPrefixName IPAM -IpamServerFqdn srv-ipam.pharma.cm

Get-GPO -All | FL DisplayName
